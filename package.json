{
  "name": "gesture-particle-light",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "vite": "^8.0.14"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
